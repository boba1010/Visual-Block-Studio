<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Visual_Block_Studio.Windows.StartWorkspaceWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Visual_Block_Studio.Windows">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar 
            BackRequested="OnBackRequested"
            x:Name="titlebar">
            <TitleBar.LeftHeader>
                <StackPanel Orientation="Horizontal" Padding="20, 10, 10, 10" Spacing="5">
                    <TextBlock Text="Visual Block Studio 2026" FontSize="20" FontWeight="SemiBold" />
                    <TextBlock 
                        Text="Preview" FontSize="15" 
                        VerticalAlignment="Bottom" 
                        Foreground="{StaticResource TextFillColorSecondary}"/>
                </StackPanel>
            </TitleBar.LeftHeader>
        </TitleBar>

        <Frame Grid.Row="1" x:Name="rootFrame" />
    </Grid>
</Window>
